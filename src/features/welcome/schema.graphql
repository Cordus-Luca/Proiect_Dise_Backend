type User {
  id: ID!
  email: String!
  name: String
  preferredTags: [Tag!]!
}

input UserInput {
  email: String!
  name: String
  password: String
}

extend type Query {
  userByEmail(email: String!): User
}

extend type Mutation{
  getOrCreateUser(input: UserInput!): User
}