type AppUser {
  id: ID!
  email: String!
  name: String
  preferredTags: [Tag!]!
}

input UserInput {
  email: String!
  name: String
  password: String
}

extend type Query {
  userByEmail(email: String!): AppUser
}

extend type Mutation{
  getOrCreateUser(input: UserInput!): AppUser
}